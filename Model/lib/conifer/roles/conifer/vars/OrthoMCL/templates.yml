# $SourceFileURL$
---

# src is relative to conifer/templates unless full path is given
model-config.xml:
  src: 'WDK/model-config.xml.j2'
  dest: '{{ gus_home }}/config/{{ project }}/model-config.xml'
model.prop:
  src: 'WDK/model.prop.j2'
  dest: '{{ gus_home }}/config/{{ project }}/model.prop'
blast-config.xml:
  src: 'EbrcWebSvcCommon/blast-config.xml.j2'
  dest: '{{ gus_home }}/config/blast-config.xml'
projects.xml:
  src: 'EbrcWebsiteCommon/projects.xml.j2'
  dest: '{{ gus_home }}/config/projects.xml'
orthomclProteomeSvcServer.prop:
  src: 'OrthoMCLWebsite/orthomclProteomeSvcServer.prop.j2'
  dest: '{{ gus_home }}/config/orthomclProteomeSvcServer.prop'
orthomclProteomeSvcCluster.prop:
  src: 'OrthoMCLWebsite/orthomclProteomeSvcCluster.prop.j2'
  dest: '{{ gus_home }}/config/orthomclProteomeSvcCluster.prop'
tomcat_context.xml:
  src: 'WDK/tomcat_context.xml.j2'
  dest: '{{ gus_home }}/config/{{ webapp_ctx }}.xml'
rebuilder.conf:
  src: 'EbrcWebsiteCommon/rebuilder.conf.j2'
  dest: '{{ gus_home }}/config/rebuilder.conf'
